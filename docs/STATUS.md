# STATUS —— 项目进度、关键决策与已知问题

> 更新:**2026-09-17** · 面向仓库读者(评审 / 面试官)

## 〇、当前版本小结(2026-09-21 · tag `v1.0-final`)

> 本节写于收尾轮(A22/A23 重跑)当时。此后 **A24/A25 只动页面与样式、没动链路**;
> 最终交付 tag 是 **`v1.0-final`** = 本节内容 + 页面 UI v3 + 两轮文档校准(遗留问题整表对账)。

**这是什么**:板子工程收口版 —— 实时画面(MJPEG)+ 回放(列表 / `Range` 拖进度条 / 看最新 /
锁定 / 播放列表)+ 页面数据面板, 全部**零安装**(浏览器 + VLC)。

**本轮(收尾轮)重跑过的验收**:
- **A22 实时画面 16/16 ✅**(默认档位改成 **20fps**:实收 **18.7fps**、每帧 **12.8KB**
  ≈**1.9Mbps**;主路 26.5~30.9fps;**"连上不读"的客户端 12 秒内被丢、通道被关、主路不受影响**)
- **A23 数据面板 + `/stats` 7/7 ✅**(段数与卡上文件对账 178==178、磁盘 23%、采样环在走、
  页面 JS 过 `node --check`)

**回归(本轮已重跑, 全绿)**:A19 列表/分页/锁定 **12/12**、A20 看最新 **17/17**、A23 数据面板 **7/7**、A22 实时 **16/16**(在**默认 20fps** 下)。更早的 A4~A18(录制/掉电/环形/多客户端)本轮未重跑 —— 本轮没碰那些代码路径。 —— 本轮改过 `svc_http.c` / `svc_live.c` /
`svc_record.c` / `svc_http_page.c`。回归清单(下一步就跑):
`work/verify_list_fix.py`(列表+分页+锁定) / `work/verify_recent.py`(看最新) /
`work/verify_stats_panel.py`(数据面板)。

**已知未做(面试请如实说)**:
1. 实时帧率**上限**未量准(默认 20fps 有实测支撑;25/30 只是扫描脚本报过数, 量法不可信);
2. **4 个 MJPEG 客户端同时看未压测**(代码上限是 4, 第 5 个回 503;已实测的是 1 个客户端);
3. 端到端延迟**只有估计**(150~250ms 的分量相加)—— MJPEG 那一路**没有 OSD 水印**, 无法对表;
4. 回放服务**串行**(一次服务一个请求, 见 ADR-5):一个人下载大分段会占住服务;
5. **无鉴权、无 TLS**(局域网演示定位, 已如实写在 README);
6. **H.265 不能录**(mp4v2 缺 `hvcC`/`hev1`, 见 `docs/问题与解决记录.md` 的说明);
   **1080p 录像也没做**(录像走 720p H.264)。

---

> 本文件只放**可公开的项目状态与决策**;个人学习笔记与本地环境细节不在本仓库。
> 所有设备地址写成占位符 `<board-ip>` / `<pc-ip>`,请替换成你自己的。

---

## 一、里程碑状态

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 | 环境搭建 + 首次出流 | ✅ |
| M1 | RTSP/RTP 服务端(取流 → 封装 → 推流) | ✅ 端到端跑通,**M1-11 多客户端已实测(2026-09-19)** |
| M2 | OSD 时间水印(码流层叠加) | ✅ 板子实测 |
| M3 | MP4 分段录制 + 环形覆盖 | ✅ 验收 **A4~A16** 全过 |
| M4 | PC 拉流端(实时预览 + 回放) | 🔄 **服务端回放 + 零安装客户端已通(A17/A18)**:浏览器页面 / VLC 播放列表 / REST 接口都能用;**自研 C 客户端(SDL2+FFmpeg)是可选加分项** |

任务分解见 [`PROJECT_PLAN.md`](PROJECT_PLAN.md) 第四节;架构见 [`ARCHITECTURE.md`](ARCHITECTURE.md)。

### M3 的验收证据(可复现, 都是实测数字)

| 编号 | 验的是什么 | 结果 |
|---|---|---|
| A4 | 录出来的文件**能被标准播放器播** | `ffprobe`: `codec_name=h264 / 1280x720 / nb_frames=871 / duration=29.033s` ⇒ 871 ÷ 29.03 = **30.0 fps**,与编码一致 |
| A5 | **环形覆盖**真的删最旧 | 日志出现 `环形覆盖删除最旧分段 …(4772963 字节)`;计数 `录制 6 段/1819 帧 删 3 丢 0 错 0` |
| A6 | **优雅关闭 vs 强杀**的差别(判别实验) | `kill -TERM` → 4 段**全部可播**(含正在写的那段 2.400 s);`kill -9` → 前 3 段可播,**最后一段 `Invalid data`** |
| A7 | **边看边录**(有人在拉流时录制不掉帧) | 客户端拉流 20 秒(解出 601 帧)期间,录制仍是 **30.28 fps**、丢 0、写错 0 |
| A8 | 分段边界**不丢帧** | 每段"日志帧数" == "文件真实帧数"(600/600);`取流 − 录制` 在 4 段时 29 帧、**9 段时仍是 29 帧**(只差开机等第一个 IDR) |
| A9 | **正在写的段不可见 + 掉电后能救回** | 录制中目录里只有 `<stamp>.mp4.tmp` 与 `<stamp>.h264.tmp`(没有 `.mp4`);`kill -9` 后残留的 `.mp4.tmp` 报 `moov atom not found`,而**同一时刻的 `.h264.tmp` 解出 315 帧**;重启后 `.mp4.tmp` 被清掉、**`.h264.tmp` 被重封成可播的 `<stamp>.mp4`**(见 A12) |
| A10 | **盘满时自动清理, 录制不停** | 把 SD 卡卸下、原地挂 **24 MB 的 tmpfs 小盘**(29 GB 卡填不满),并在 `-r 0` **关掉环形上限**的前提下:收尾 8 段、**提前清理 6 次**、**写入失败 0 次**、进程存活;换成 8 MB 盘 + 一段约 40 MB(一段比盘子还大)时,**如实报错且不崩不卡** |
| A11 | **RTP over TCP 交错真的能拉流** | `ffmpeg -rtsp_transport tcp` 拉 15 秒:服务端 **发送帧 +454**(修复前 **+0**)、`tcp_send` 失败 **0** 次、客户端文件 **452 帧 / 1280x720 / 15.03 秒可播**;同一次里 UDP 对照 +405 帧、文件同样可播 |
| A12 | **开机时板子自己把断电残留救回来**(不需要 PC) | 录 150 秒 → `kill -9` 造残留(裸流侧车 81,010,557 B、`.mp4.tmp` 80,870,968 B)→ 重启:启动清理**只删 `.mp4.tmp`**(日志"共删除 1 个 MP4 半成品",**没有**任何删 `.h264.tmp` 的行)、裸流被重封成 `2026-09-19-20-59-55.mp4`(**81,025,798 B**,比裸流多 15 KB = 索引)、`grep -ac moov` = **1**、拷到 PC 后 `ffprobe` = **`h264 / 1280x720 / nb_frames=4658 / duration=155.27 秒`**;重封耗时 **13 秒**。⚠️ 代价:重封期间录制队列被挤满 ⇒ **丢 173 帧**(≈5.8 秒实时画面),按 30 分钟一段线性外推约丢 2 分钟(见 `svc_record.c` 的【简化上限】) |
| A13 | **多客户端同时拉流**(M1-11,收口 M1) | 在 VM 上**同时**开 3 个客户端(2×UDP + 1×**TCP 交错**)各拉 20 秒:服务端峰值 `客户端 3 在发`、`发送帧 +1812`(= 3 × 604);三个客户端**各自** `h264 / 1280x720 / 602 帧 / 20.03 秒`、文件大小**都是 10,501,920 B**;期间录制 **30.32 fps、丢 0 帧**、错误 **+0**;客户端退出后 `客户端 0 在发`。⇒ "单发送线程轮询多客户端"这条路验证通过(不需要每客户端一线程) |
| A14 | **分段大小上限**(时间到**或**大小到, 谁先到算谁;边界仍对齐 IDR) | **两个相位互相对照**(各只动一个自变量):**A 大小触发** `-s 1800 -m 8` —— 6 个满段 **8,419,032 ~ 8,907,138 字节**(上限 8 MiB), 480~510 帧 / 16~17 秒, 时间上限(30 分钟)根本没到;**B 时间触发** `-s 20 -m 1024` —— 5 个满段 **600~628 帧 / 20.00~20.93 秒**(≈10.4 MB ≪ 1 GiB), 大小上限根本没到。**13 个分段逐个对账全过**:① 每个文件都能 `ffprobe` 读出帧数(边界仍对齐 IDR);② **日志帧数 == 文件帧数**(如 510 vs 510、396 vs 396);③ **日志"实际文件字节" == `stat` 真实字节**;④ MP4 封装开销实测 **0.06%**。满段超出上限的零头 **+0.36% ~ +6.18%** —— 正是"等下一个 IDR"的那几帧(理论 ≤1 个 GOP)。收段本身有成本:5 次收段落 **2 帧**(0.06%) |
| A15 | **锁定段保护**(锁定的分段环形覆盖不删) | **三相位,每个只动一个自变量**(`-s 1800 -m 8 -r 40`, 8 MB 一段 / 环上限 40 MB ⇒ 清理频繁):**A 对照(不锁测试段)** 33→2 个分段、**删 38 个**, 且**第一个删的正好是最旧的那个**(`2026-09-19-20-54-20.mp4`)⇒ 基线"环删从最旧开始"成立;**B 锁定当前最旧的那段** ⇒ 它**活下来了、且不在删除日志里**, 而被删的是别的 6 个(遇到锁继续删下一个, 不是卡死);**C 把全部段锁上** ⇒ 日志明确报 `已超环形上限但没有可删的分段`, **进程仍在跑、锁定的段一个没丢**。全程把用户那份 248 MB 老录像锁着(它最旧, 不锁第一个就被删) —— A/B 两相位它都在 |
| A16 | **仓库自带 `Makefile`**(别人拿到仓库能不能编) | 只把**仓库里真实存在的** 59 个文件(Makefile + `src/**` + `tools/**`, 不带任何开发环境脚本)传到干净目录:**① `make` 用默认变量 → `build/ipc_app`**(16 秒, 我们自己的代码 **0 告警**, 二进制 4,980,270 字节);**② `make test` → RC=0**(宿主 gcc, **不需要 SDK/板子**;7 条跑完, 2 条需要裸流样本的**显式跳过**并打印跑法);**③ `make test RAW_STREAM=<真裸流>` → RC=0**(9 条全跑, 0 告警);**④ 把 `make` 出来的二进制推上板真跑**:录到 6 段, 最后一段 `ffprobe` = `nb_frames=209 / duration=6.97 秒`(可播);**⑤ `make clean` 删干净 `build/`**。顺带被这个测试目标抓出两处:**`svc_net_test` 在 B036 之后腐烂**(回调签名没跟着改, 2 条断言失败却只有一条 warning)已修并**补上 4 条 TCP 交错传输断言**(62 项全过);`rtp_test`/`sdp_test` 本来就需要外部裸流样本。**加了回放那套代码之后又重跑了一遍**(A18 当天):仍全绿 —— `wildcard src/*/*.c` 自动把新模块收进去了 |
| A17 | **阶段 2 回放服务(HTTP)**:列分段 / `Range` 取流 / 锁定解锁 | 板子上起 8080 服务,从 VM 用 curl/ffprobe 打 **20 条判据, 20/20 通过**:① `GET /recordings` → JSON, **64 条分段、新→旧**,每条带 `name/size/locked/uri/lock_uri`;② **整段取流与卡上文件 `md5` 完全一致**(8,873,009 字节,`f1e704a7…`);③ 四种 Range 都对:**`0-1023`**、**`1000000-1000999`**、**`-100`**(后缀)、**`100-`**(到结尾)—— 每条都是 206 + `Content-Range: bytes a-b/8873009` + **与"整段文件里对应那段"md5 相同**;④ 越界 `-r 8873009-` → **416 + `Content-Range: bytes */8873009`**;多段 `0-9,20-29` → 200(刻意不支持);⑤ 该拒的都拒了:`.locked` / `*.mp4.tmp` / `../`穿越 / 不存在 → **404**,`PUT /recordings` 与 `HEAD` 列表 → **405**;⑥ `HEAD` → 200 + 正确 `Content-Length`(不下 body);⑦ **`ffprobe` 直接读 HTTP 成功** =`h264 / 1280x720 / nb_frames=510 / 17.00 秒`(我们的 `moov` 在文件末尾,能读出来就说明 **Range 真的被播放器用上了**);⑧ 锁定/解锁:`PUT .../lock`(无 body)=锁、`-d 0`=解锁,列表里 `locked` 跟着变,`.locked` 文件同步;⑨ ★ **挂一个"只连不读"的客户端**:别人仍能 200,且 `kill -TERM` **3.4 秒退出**(日志有 `tcp_write_all: 等可写超时` —— 说明是服务**主动丢连接**而非被拖死);⑩ 全程录制不受影响:`取流 − 录制 = 29`、**丢 0 / 错 0** |
| A18 | **零安装客户端**:整段播放列表 + 板子自带网页 | `GET /playlist.m3u`(给 VLC/mpv 的**整段**回放列表, **旧→新**)与 `GET /`(板子自己发的**回放页面**:分段列表 + `<video>` + 锁定/另存按钮 + 时间轴雏形 + 直播地址),`/help` 仍是纯文本。**17/17 条判据通过**:① 列表 `#EXTM3U` 开头、**64 条 URL、顺序旧→新**(首条正是那份 09-18 的 248 MB, 末条是最新段)、每条都是**绝对 URL**且有 `#EXTINF`、点名的 URL 真能取(206 + 100 字节);② `/` 是 `text/html`、含 `<!DOCTYPE html>`/`<video>`, 页面里出现 `/recordings`、`lock_uri`、`playlist.m3u`、`rtsp://`;**页面脚本过 `node --check`**(2127 字节语法通过 —— 页面写坏只会白屏, 所以单独查);③ **Host 替换实验**:用 `Host: board.local:9999` 请求 ⇒ 列表里的 URL 跟着变成 `http://board.local:9999/…`, 证明**没有硬编码板子 IP**;④ 老接口没坏(`/recordings` 200、`/help` 200、不存在 404);⑤ 页面按钮要调的接口实测:HEAD 200、Range 206 |

| A19 | **修掉"列表只看到几小时前"**(B044)+ 新增 `/status`(正在录的那一段)+ 页面自刷新 | 用户报"刷新了也不行, 回放最新只到 22:59"。查出来是**真 bug**:列表接口的分段数组只有 **64 格**, 旧实现"**先填满 64 格再排序**"⇒ 留下的是 `readdir` 顺序里先遇到的 64 个(恰好都是旧的), **真正最新的段根本没进列表**, 所以刷多少次都一样。修法:扫目录时**只保留最新的 top-K**(满了拿"当前最旧"出来比较替换)+ 新增 `?limit=&before=` **分页**。另加 `GET /status` 报出**正在写的那一段**(名字/字节/帧数/起始时间)与页面 **5 秒自刷新** + "加载更早" + "● 正在录"横幅。**12/12 条判据通过, 全部拿卡上实际文件对账(不看程序自报)**:① 列表第一条 == 卡上 `ls \| tail -1` 的最新 `.mp4`(当时 **`2026-09-20-…` 最新段**)✅;② `count` == `ls -1 *.mp4 \| wc -l` = **126**(修前只有 64, 且不含最新)✅;③ `?before=<第一页最后一条>` 拿到**更早的一批**、全部早于它、**与第一页零重复** ✅;④ **`/status` 指的正是卡上那个 `.mp4.tmp`**(名字一致、`bytes`>0 且与录制同步增长、`started_at` 在合理区间)✅ —— 这一段就是**.tmp 阶段对外不可见**的那部分(见 §二"缺口"那条);⑤ 页面含 `/status`/`加载更早`/`自动刷新`/`正在录`, 且**从板子实际发出的字节**里抽出 JS(**4004 字节**)过 `node --check` ✅;⑥ 老接口没坏:整段 **md5 与卡上文件逐字节一致**(拿最小那一段对账)、`Range 0-99` → **206**、锁定/解锁往返 `"locked":1`→`"locked":0` ✅。缓存头:页面/列表 `Cache-Control: no-store`(所以"刷新"拿到的永远是新的), 分段文件 `Accept-Ranges: bytes` + `no-cache`。**改完的回归**:把仓库内容(Makefile + `src/` + `tools/`)重新同步到干净目录跑 **`make test` = RC 0 / 0 告警 / 0 失败**(2 条需要裸流样本的显式跳过);交叉编译**我们自己的代码零告警**, `ipc_app` **4,980,364 字节** |

| A20 | **`GET /recent.mp4`:"刚录的这段"立刻能看**(补上"正在录的段不可播"那个缺口) | 做法**不是**自己重封一段, 而是**请录制线程在下一个 IDR 处把当前段收尾**(复用已有切段路径 ⇒ 边界仍对齐关键帧、一帧不丢), 再把**刚收尾的整段**发出去(带 `Range`)。上板 **17/17 条判据通过**, 每条都与**卡上文件/日志**对账:① 请求前 `/status` = `2026-09-20-15-18-01.mp4`(段龄 28 秒), 请求后正在录变成 **`…-15-18-29.mp4`**(真的切了), 卡上已收尾段 **140 → 141**, 那个 `.mp4.tmp` **消失**、`.mp4` 出现 ✅;② 应答 `200` + **`X-Recent-Clip: 2026-09-20-15-18-01.mp4`** + `Content-Type: video/mp4`, 且 **`Content-Length` = 14,444,662 == 卡上 `ls -l` 的真实字节** ✅;③ 它就是列表**第一条** ✅;④ **能播**:`ffprobe` 直接读 HTTP = `nb_frames=840 / duration=28.000 秒`, 与日志"分段收尾 … **840 帧**"**完全一致**(产物对账)✅;⑤ `Range 0-99` → **206** + `Content-Range: bytes 0-99/14444662` ✅;⑥ **反面对照(段太新不切)**:紧接着再请求 ⇒ 仍给同一段、卡上段数 **141 → 141**(没有把刚开的几秒新段切碎)✅;⑦ **反面对照(没在录)**:`-no-record` 起一次 ⇒ `/recent.mp4` = **503** ✅;⑧ 页面多了"看最新"按钮, 页面 JS(4503 字节)过 `node --check` ✅。⚠️ 代价如实说:每请求一次**多一个分段边界**(所以有 20 秒"段太新就不切"保护),并会**占住服务线程最多 5 秒**(串行模型) |
| A21 | **命令行开关"按文档写法"重新验收**(顺带抓出 B045) | 用法/README 里写的一直是**单横线**长选项(`-no-record` / `-h265` / `-no-osd` / `-no-raw` / `-no-http`), 而 B040 那次"修复"只把 `getopt()` 换成 `getopt_long()` —— **glibc 的 `getopt_long()` 只认双横线**, 实测 `-no-record` 仍然是 `invalid option -- 'n'`(**文档里的写法依旧全废**, 而代码注释里那句"接受单横线"是没验过的断言)。修法:解析前用 `normalize_long_opts()` 把**已知名字**的单横线写法改写成双横线(`getopt_long_only()` 会让 `-h` 与 `-h265` **前缀歧义** ⇒ 砸掉 `-h`)。上板 **12/12 判据通过**:`-no-record`/`--no-record`/`-no-osd`/`-no-http`/`-h265` 逐个跑, **看启动横幅那一行真的变了**;`-h265` 时日志里出现"录制: ⚠️ 关闭(mp4v2 只支持 H.264)";`-no-http` 时 **8080 真的连不上**(curl → 000);短选项没坏(`-p 8599` 出现在监听行);`-H 8099` 时 **:8099/help → 200**;`-h` = 用法 + **退出码 0** + 不启动链路;`-x`(未知) = 用法 + **退出码 1** |

| A22 | **MJPEG 实时画面进浏览器**("实时"和"回放"终于都在一个页面上) | 先做**孤立实验**(`tools/jpeg_smoke.c`)再产品化。实验结论(上板实测, 主路基线 30.0 fps):① **编码类型必须用 `PT_MJPEG`** —— 自己拼 `PT_JPEG` 的 attr 建通道, 驱动回 `0xa0088003`;用交叉编译的探针在板上打印才知道那是 **ILLEGAL_PARAM**(我一开始猜成 BUF_EMPTY, 两者只差一位);② **一个 VPSS 通道可以双绑两个 VENC 通道**(实测成功), 另加一路 VPSS chn2(640x360)也成功 ⇒ 产品里优先小图那路、失败回退双绑;③ 通道**空闲态**(建好绑定但不开收流)主路 **30.2 fps**、**活动态** 29.8 fps、**拆掉后** 30.0 fps —— 都不掉;④ ★ **开着收流但 6 秒没人取**(B027 场景)主路 **30.2 fps, 一点没掉** —— 看起来 MJPEG(每帧独立)是"缓冲满就丢帧", **不像 H.264 那样反压 VPSS**;⚠️ **但设计不依赖这个结论**(没人看就整条拆掉, 理由写在 `svc_live.h` 文件头)。产品实现:`svc_live`(**独立端口** 8081 + 独立线程 `ipc_live`, 单线程 poll 多路复用, 一帧只取一次广播给 ≤4 个客户端;客户端**不缓存待发数据**, 写不动就丢)+ `bsp_mpp_mjpeg_open/close/get_frame`(**按需启停**:第一个客户端来才建通道, 最后一个走就拆)。**验收 16/16 通过**(判据全来自产物/内核):① `:8081/live.mjpg` → `200` + `multipart/x-mixed-replace; boundary=ipcframe`;② **66 片 / 7.8 秒 = 8.5 fps**, 每片平均 **19,687 字节**(17,776~21,924), 且**每一片都是 `FF D8 … FF D9` 的合法 JPEG**(不是"取到字节"就算过);③ 同一时间窗主路 `/proc/umap/venc` 的 Sequence 差 = **26.3 fps**, 录像 8 秒长 3.3 MB ⇒ 推流/录制都没被牵连;④ 同一时间窗 `/recordings` → **200**、`count=165` ⇒ 两个服务互不干扰;⑤ **按需开**:连之前 `/proc` 里无 chn2, 连上之后**有**且日志有"MJPEG 通道就绪";⑥ ★ **挂一个"连上不读"的客户端 12 秒**:日志出现"写不动了"、通道被关且 `/proc` 里 chn2 消失, 而这 12 秒主路仍 **30.9 fps**;⑦ 反面对照 `-no-live` ⇒ 8081 **连不上**且 `/status` 无 `live_mjpg`;⑧ 页面有"看实时"按钮, 页面 JS(5115 字节)过 `node --check`。⚠️ 代价如实说:每请求一次多一次通道建/拆;客户端太慢会被断开(浏览器会自动重连, 见【简化上限】);实时流是**长连接**, 所以单开端口/线程, 不去挤那个被验证过的串行回放服务 |

| A23 | **页面"运行数据"面板 + `GET /stats`**(用户要的"多点数据图";范围收在**这个本地页面**上) | 后端**不新增线程、不碰 MPP**:`GET /stats` 把 media/sender/net/record/live **已有的**统计快照 + `statvfs` 磁盘 + 录制目录汇总一次吐成 JSON;另加 **1 Hz × 60 的采样环**(文件级静态 ~1.5KB,把累计计数器化成"这一秒的速率")—— ⚠️ **谁拉 `/stats` 谁顺手采**, 没人看页面就不采(不新增采样线程)。前端仍是**一个常量字符串、零构建、不引任何外部资源**:8 张 KPI 卡 + **手绘 sparkline**(Canvas, 不引图表库)+ **存储条**(已用/总量/剩余 + 按当前码率"还能录 ~N 小时")+ **客户端表**(RTSP / MJPEG / 回放 HTTP / 录制四行, 顺带把"**一帧编一次共享**"和"回放**串行**"这两个架构事实写在页面上)。**上板 7/7 通过**:`/stats` 200 + JSON 字段齐全;`segs.files` == 卡上 `ls -1 *.mp4 | wc -l`(**178 == 178**, 与文件系统对账);磁盘 **29.7 GiB / 剩 23.0 GiB / 已用 23%**;`hist` 两次请求之间**变长**且 dt 合理;`rec.cur_bytes` 与 `/status.bytes` 方向一致、差值落在"这 0.2 秒内合理增长"内(31,541 ≤ 629,145);页面 21,369 字节、数据面板元素齐全、内联 JS(12,044 字节)过 `node --check`。⚠️ 一处判据被我自己修正: 最初要求两个接口的 `cur_bytes` **完全相等** —— 那是错的(它测的是**正在长**的计数器, 两次请求之间必然变大; 第 9 次踩"口径"坑)。⚠️ 页面观感仍需人眼确认 |
| A24 | **页面样式基底:内联 Water.css v2 dark + 新增 `/app.css` 端点**(承接 A22 后续那句"UI 还是太丑") | 选型依据:用户要"去 GitHub 找热门的 UI 做法" ⇒ Pico.css 压缩后 **83KB**(太胖)、Tailwind 需要**构建步骤**(与"一个常量字符串、零构建"冲突)⇒ 用 **Water.css v2 dark(10,106 字节, MIT)** 当"控件与排版的地板"(按钮/表格/输入框/code/滚动条), 页面自己只再写大屏布局层。**不引外部资源**(板子常没外网):CSS 由**板子自己**用 `/app.css` 发出去, 带 `Cache-Control: max-age=86400`(页面 HTML 不变大、样式可被浏览器缓存), 页面本身仍 `no-store`;实现上把页面与样式**合并**成一个 `send_static()` + `handle_asset()`(顺带把 `dispatch` 从 **51 行压回 48 行**)。**上板验证**:① `/app.css` → **200** + `Content-Type: text/css; charset=utf-8` + **`Content-Length: 10106`**(== 生成器算出的字节数)+ `Cache-Control: max-age=86400` ✅;② 落地文件 **10,106 字节**, 首字节就是 MIT 许可注释(`/*! Water.css v2 (dark) — MIT License …`)、结尾 `oration:underline}}` ✅;③ 页面(**22,665 字节**)里确实有 `<link rel='stylesheet' href='/app.css'>` ✅;④ **A22 回归 16/16、A23 回归 7/7** 仍全绿 ✅;⑤ 可复现:上游 CSS **原样入库**(SHA256 `143F43E9…675C24`)、`tools/gen_page_css.py --check` 幂等、交叉编译**我们自己的代码零告警** ✅。⚠️ 本轮只落地"样式地板" —— 大屏布局层的视觉统一与截图审计(`web-design-guidelines` / Playwright)还没做 |
| A25 | **页面 UI v3(监控大屏风)+ 页面"源码化"**(用户:"UI 还是太丑, 尽量做 ui") | ① **结构**: 页面源码从"C 字符串里手写 HTML"改成 `tools/assets/page.html` →(生成器 `tools/gen_page_html.py`)→ `svc_http_page.c`。**为什么值得**: page.html 是标准 HTML(双击能看、能直接喂无头浏览器), 于是 UI 迭代**不必先上板** —— 生成器还负责硬约束(每行 ≤100 显示列 / 转义 / LF)与 `--check` 幂等。② **视觉**: 显式夺回 Water.css 给 `body` 的 `max-width:800px`(**这就是大屏一直没铺开的原因**); 画布 1560px、吸顶毛玻璃栏、10 张 KPI 一条铺满、16:9 画面区 + 时间轴、通道现状表, 窄屏 ≤560px 自动堆叠。③ **可访问性**(按 `vercel-labs/web-interface-guidelines` 逐条): 时间轴色块与列表行改成真 `<button>`(原来是带 onclick 的 `div`)、图标按钮 `aria-label`、toast `aria-live=polite`、`:focus-visible` 焦点环、`prefers-reduced-motion` 关动效、`color-scheme:dark`+`theme-color`、表格 `caption`、skip link。⚠️ 一条**没采纳**: "列表 >50 项要虚拟化" —— 试过 `content-visibility:auto`, 它让**全页截图里视口外的行变空白**(`innerText` 也读不到), 对"截图进简历"是净损失, 撤掉(理由写在生成器文件头)。④ 顺带: 深链 `#s=<段名>`/`#live`(状态进地址栏可分享)、内联 data: SVG 图标(消掉 favicon 404)。**上板 25/25 判据通过**(桌面 + 390×844 窄屏 + 深链, 0 运行期报错), 回归 **A19 12/12 · A20 17/17 · A22 16/16 · A23 7/7**。⚠️ 修掉两个**量出来**的 bug: `addRow()` 里 `data-name` 用在 `var main` 之前(var 提升 ⇒ `undefined.setAttribute`, 列表整体不渲染, 板上表现"KPI 有、列表空")、正在录那一行 `i=-1` 撞上 `sel=-1` 被误标选中(量到底色是 `.sel` 的 `#152a3d`)。截图证据: `work/board_dash.png` / `board_playing.png`(真录像+OSD) / `board_live.png`(真 MJPEG) / `board_mobile.png` |
| A26 | **回放列表的时间戳补上日期**(用户反馈:"这个时间……不然不知道是哪一天的") | 根因: 页面里 `t9(){return s.substr(11,8)}` —— 从段名 `2026-09-21-21-01-46.mp4` 第 11 位起取 8 字符, **正好把日期切掉**, 列表只剩 `21-01-46`。改成 `dt()` 返回完整时间戳 **`2026-09-21 21:01:46`**(冒号, 与主流 NVR/IPC 界面一致), **10 处调用点全换**(列表行 / 顶部"正在录"横幅 / 运行数据表 / 时间轴 `title` / 视频角标 / 锁定·下载 toast / `#cur`);长度不足 19 的异常名**原样返回**, 不截不猜。**验证**: 本机假板子 **25/25** + **真板子 25/25**(第 2 行 = `2026-09-21 21:01:46`、窄屏同样、深链与回放不受影响), 截图 `work/ui_dash.png`;⚠️ 其中 **3 条判据是特意改强的** —— 原来断言"8 个字符且第 3 位是 `-`"(钉的是旧短格式), 现在必须匹配 `\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}`, 回归成短格式会立刻红 | ✅ 2026-09-21(提交 `fdf519e`;新二进制已装进 rootfs, md5 `67a4f126…`) |

> A6/A7/A8/A13 都是**判别实验**(带阳性对照、有明确证伪判据),不是"跑起来看着正常"。

### A22 后续(2026-09-20 用户反馈"帧率低、延迟大、页面丑")

用户反馈三件事, 逐条查了(不猜):

| 反馈 | 查证 | 结论/处理 |
|---|---|---|
| 画面"撕裂"(像四块拼起来) | 从流里存下 10 帧, **逐帧看**:单帧 640x360 内容完整、无花屏(`work/live_v2_0.jpg`) | **帧本身没问题** ⇒ 是浏览器在 `multipart/x-mixed-replace` 下**边收边画**时被截到的中间态(或用户当时看的是 `<video>` 播放中的录像)。**不是服务端 bug** |
| 帧率低 | 量到 **9.0 fps**, 片间隔中位 **110 ms**;再量"编码器编了多少 vs 我们发了多少":**编码器 9.1 fps / 发送 10.4 fps** | 上限卡在 **`fr32DstFrameRate = 10`**(编码器侧降帧), 不是卡在取流。改成 **15**(默认)+ 节拍 100→66 ms ⇒ 复测 **17.2 fps、间隔中位 70 ms、每帧约 15.3 KB**;主路仍 31.5 fps |
| 延迟大 | 端到端**没法直接量**(见下), 只能把**每个分量**量出来相加 | 编码器 ≤1 帧周期(66 ms)+ 我们节拍 ≤66 ms + 局域网传 15KB(<10 ms)⇒ **估计 150~250 ms**。⚠️ **诚实标注:这是"分量相加的估计", 不是实测的玻璃到玻璃延迟** |

- ⚠️ **`MJPEG` 那一路没有 OSD 水印**:我们的时间水印(`bsp_osd` 的 REGION)只挂在**主编码
  通道**上, 而 MJPEG 是**另一路 VENC 通道**(chn2) ⇒ 实时画面里没有时间戳。
  这也是"延迟没法直接用 OSD 对表"的原因。升级路径: 开 MJPEG 通道时**也给它挂一个
  REGION**(`HI_MPI_RGN_AttachToChn` 支持多通道), 现在没做。
- **页面重做(UI v2)**: 用户说"太丑、点击逻辑差"。新版按主流 NVR 控制台的样子重写
  (参考 Frigate 的深色卡片风、Shinobi 的"时间轴 + 录像列表"布局):
  深色卡片 + 状态胶囊(正在录/连不上) + 16:9 播放区(**实时与回放共用**, 带角标/抓图/全屏)
  + 右侧录像列表(行内操作、悬停与选中高亮、窄屏堆叠到下方) + 按**文件大小**分配宽度的
  时间轴(悬停显示段名、右侧"正在录"红标) + **toast 通知**(不再用红条和 `alert`)
  + 忙碌态禁按 + 乐观更新失败回滚 + `localStorage` 记住"自动刷新" + 键盘快捷键
  (空格/←/→/L/R/F)。
  ⚠️ **不引任何外部资源**(板子通常没有外网 ⇒ CDN/字体/图标库一律不用), 12KB 一个常量字符串,
  没有构建步骤。实测: 页面 **16,551 字节**、内联 JS **8,648 字节**过 `node --check`。
  验收脚本 `work/verify_ui_and_live.py`(**4/4**)。

- **样式基底(A24, 2026-09-20 深夜)**: 用户接着说"UI 还是太丑", 于是去 GitHub 挑了样式底座
  —— 最终**内联 Water.css v2 dark**(`/app.css` 端点, **10,106 字节**, 可缓存一天),
  页面只再写大屏布局层; 并补上**真的跑得起来**的生成器
  `ipc_camera/tools/gen_page_css.py`(上游 CSS **原样入库** + 注入 MIT 许可注释 +
  按**显示宽度** ≤100 列折行 + `--check` 校验幂等)。
  ⚠️ 修掉的前一版问题: 头文件里写着"用 `work/gen_page_css.py` 再生成", 而**那个脚本不存在**,
  它声称的"许可声明就是第一行"也不成立(首行是 `:root{`, MIT 要求保留版权声明) —— 记入 **B049**。
  上板判据见验收表 A24(`/app.css` 200 + 10,106 字节 + `text/css`;页面含 `<link>`;
  **A22 16/16、A23 7/7** 重跑仍绿)。⚠️ 大屏布局层的视觉统一与截图审计还没做。

- **UI v3 + 页面源码化(A25, 2026-09-20 深夜)**: 用户接着说"尽量做 ui"。这一轮的**方法**比
  样式本身更值钱 —— 把页面挪进 `tools/assets/page.html`(标准 HTML), 用
  `tools/gen_page_html.py` 生成 C 字符串; 再配两个本机工具:
  `work/preview_server.py`(假板子, 按板子的 JSON 形状造假数据, 并把"页面到底调了哪些接口"
  打成**服务端访问日志**)与 `work/shot_page.py`(Playwright 驱动**系统自带 Edge**,
  不下载 chromium: 截图 + 抓 console/pageerror + 真点按钮断言 DOM)。
  ⇒ **20 秒就能验一遍页面, 不必先上板**; 真上板前先跑本机这一遍, 已经当场拦下一个
  `var` 提升引起的 TypeError(见 B050)。
  设计上最实质的一处: **Water.css 给 `body` 的 `max-width:800px` 一直压着整个布局** ——
  之前"页面像手机版"就是这个原因, 现在显式夺回。截图见 `work/board_*.png` 与 `work/mock_*.png`。

---

## 二、关键设计决策(附理由)

| 决策 | 理由 | 备选与为什么不选 |
|---|---|---|
| **分段边界对齐到 IDR(关键帧)** | "数够 N 帧就切"会在刀口上丢帧 —— 新段建轨需要 SPS/PPS(跟着 IDR 一起来),等它到达期间那些帧就被丢掉了(实测每段丢 29 帧 ≈ 1 秒)。改成:**收满后继续写当前段,遇到下一个 IDR 才关旧段,并用这一帧开新段** | ffmpeg `segment` muxer 的官方行为也是"从指定时间**之后第一个关键帧**开始",与此一致 |
| 默认**每段 30 分钟**;环形上限 `-r` **必须大于一段** | 30 分钟是监控行业惯例(NVR/DVR 普遍 30 或 60 分钟)。⚠️ 上限若小于一段,环形覆盖会**静默退化成"只留一段"** | 段太短 → 文件数爆炸;段太长 → 掉电一次丢太多 |
| ⭐ **分段依据 = 时间到 或 大小到(谁先到算谁)**, 默认 30 分钟 / 1024 MB | 只按时间切时, 段大小 = 时长 × 码率, 而码率随画面复杂度浮动 ⇒ 有可能冲破 vfat(FAT32)的 **4 GiB 单文件硬上限**, 那会在**段中间**写失败。加一条大小上限, 文件大小就有确定性上界。判据用的是**真正落盘的码流字节数**(不是"处理过多少") | ffmpeg `segment` muxer 只有按时间/分片数;海康 SDK 是**按字节切**。我们**两个都要**, 因为两者的失效模式不同(时间→掉电丢得多;大小→撞 FAT32 上限)。**大小上限故意不提供"关闭"**(配置里 0 = 用默认), 要放宽只能设接近 4 GiB 的值, 启动时告警 |
| **环形覆盖**按文件名排序删最旧 | 文件名零填充 `YYYY-MM-DD-HH-MM-SS.mp4` ⇒ **字典序 == 时间序** | 厂商 sample 用 `%d-%d-…`,`2026-9-16` 会排在 `2026-10-1` **后面** ⇒ 删错文件 |
| **锁定段保护**:清单文件 `<dir>/.locked`(每行一个文件名), 被锁的段**跳过不删** | 行业通用语义(海康"锁定后不会被覆盖"; ISAPI `lockStatus`)。用**纯文本清单**而不是文件属性(vfat 的 `FAT_IOCTL_SET_ATTRIBUTES` 不可移植)也不是改名(会同时破坏"字典序==时间序"和 `*.mp4` 扫描约定)。清单是**唯一真相源** ⇒ 策略层仍是纯函数(只吃一个布尔量), 阶段 2 的管理接口只需读写这个文件 | 数据库/单独目录: 都要么引依赖、要么破坏"一个目录就是全部录像"的简单模型。⚠️ 锁定段**照样占容量**: 全锁定时清理删不动, 此时明确报错而**不自动解锁**(锁是用户的意图) |
| 覆盖决策是**纯函数** + 单元测试 | "该删哪几个"与"真的 `unlink`"分离 ⇒ 决策可在 PC 上原生单测(现有 **23 条断言**) | — |
| **优雅关闭**(捕获 `SIGINT`/`SIGTERM`) | MP4 的索引 `moov` 只在 `MP4Close` 落盘;不优雅关闭 ⇒ 整个文件打不开 | 真断电无法避免 —— 只能靠**分段**把损失限在一段内 |
| **单路编码**(不做主/子双码流) | 在这颗芯片的 MPP 上,**启动一个没人取流的编码通道会把整条流水线拖死**(实测固定卡在 205 帧) —— 未消费通道的缓冲塞满 → 编码器停 → 反压到 VPSS | 真实 IPC 通常同时编 2~3 路;多路的前提是每路都有消费者 |
| VENC 通道与 VPSS 通道**成对选择**,全文件不写死通道号 | 同类错误在"切到另一路"时会整体失效 | — |
| TF 卡用 **vfat** 而不是 ext4 | SD 协会规定 SDHC(≤32 GB)用 FAT32(**合规**),且 vfat 能**拔卡插电脑直接看** | Axis 官方建议监控卡用 ext4(断电后恢复更快、更不易损坏)—— 这是**知情的取舍**,代价是寿命与空间利用率 |
| **不采用 fMP4** | ① 没找到任何厂商公开文档说机内录像写 fMP4(采用证据都在流媒体侧 DASH/HLS/CMAF);② 所用库 **mp4v2 结构上不支持** —— 14 个头文件里 `moof`/`mvex`/`tfhd` **零命中**,源码里只有一行未实现 TODO;③ 实测它的收益与"旁路裸流"相同 | 详见 §3.9 与 [`docs/research/`](research/) |
| 发送与录制用**两条独立队列**(在取流处扇出) | 磁盘慢只丢录制的帧、网络慢只丢发送的帧 | 否则"存储坏了 = 直播也看不了";单队列两个消费者会各拿一半帧 |
| **正在写的段用 `.tmp`、收尾才 `rename`** | 让"这段是否完整"成为目录里**一眼可见**的状态:目录扫描只认 `.mp4`,半成品既不进环形容量、也不出现在检索列表。依据是 Axis 边存官方的 `recording.tmp` 模式("写完才对外可见") | 只在收尾时改名;极少数情况下 `rename` 失败会留下 `.tmp`,由启动清理兜底 |
| **旁路裸流侧车** `<stamp>.h264.tmp`(默认开,`-no-raw` 可关) | 断电/强杀时 MP4 **一定**打不开(没写 `moov`),而**同内容的 Annex-B 裸流被截断也能解** —— 实测同一时刻:残段 0 帧、**侧车 315 帧**。侧车只在"这一段进行中"存在,收尾即删 ⇒ **稳态磁盘占用不变**,环形覆盖一行没改 | 写入量翻倍(卡寿命约 6 年 → 约 3 年);不调 `fsync`,断电最多丢约 30 秒侧车(升级路径写在代码的【简化上限】里) |
| **回放列表默认只给"最新一页"(64 条)+ `?limit=&before=` 翻页** | 运行期不做动态分配 ⇒ 列表得用**文件级静态数组**, 容量写死。全量返回在段数多时既撑不住也"用户根本看不出重点"; 而回放**最常看的就是最新那段** ⇒ 默认视图给最新的 64 条, 更早的用 `before=<当前页最后一条>` 翻。判据是"第一条 == 卡上最新的 `.mp4`"(`STATUS` A19) | 一次性全量返回: 段数无上限, 静态数组一定被撑爆。⚠️ 代价: **"先填满数组再排序"是个真 bug**(B044)—— 那样留下的是 `readdir` 先遇到的 64 个, 恰好都是旧的, 表现为"刷新多少次都只看到几小时前"。正解是**边扫边淘汰**(留下最新的 K 个) |
| ⭐ **"看最新"= 让录制线程切一段, 而不是自己重封一段** | 正在写的 `.tmp` 没有 `moov` ⇒ 播不了。让**已有的切段路径**提前跑一次(下一个 IDR 处关旧段、用这一帧开新段), 那一秒后就有一个完全正常的 `.mp4`:边界仍对齐关键帧、**一帧不丢**、`Range` 直接可用、**不用碰 mp4v2 的跨线程问题**、也不用估算"N 秒大概多少字节"。判据见 A20 | 被否掉的方案:**把旁路裸流侧车尾部 N 秒重封** —— ① 得按码率**估算**字节位置(估不准);② 要在另一个线程动 mp4v2(库的全局状态 + 录制线程正在用);③ 重封期间录制队列被挤满会丢帧(与 A12 同代价)。⚠️ 代价:每次请求**多一个分段边界** ⇒ 加 20 秒"段太新就不切"的保护 |
| ⭐ **"正在录的那一段看不到"是设计行为, 不是 bug** —— 用 `/status` + 页面"正在录"横幅把它**显式化**, 用**段长**控制缺口大小 | 正在写的段是 `<stamp>.mp4.tmp`(见上一条), 它不进列表/不占环形容量, 代价就是"离现在最近的这段(默认 30 分钟, 现在板上设 300 秒)在段收尾前任何客户端都取不到"。行业同样如此(Axis 的 `recording.tmp`), 因为 **MP4 的索引 `moov` 只有关文件时才写**: 没 `moov` 的文件给了播放器也打不开, 让它出现在列表里反而是**假承诺**。所以不做"假装能看", 而是: ① 新增 `/status` 如实报告".tmp 有多长、录了多少帧、从几点开始"; ② 页面顶部横幅显示"● 正在录 …"和"最新可看 = 上一段"; ③ 想缩小缺口就**把段长调短**(`-s 300` = 最多落后 5 分钟), 这是唯一正确且无副作用的旋钮 | ① 把 `.tmp` 当 `.mp4` 列出来: 用户点了必然打不开(没有 `moov`)。② 边写边发 fMP4(`moof`): 需要分片索引, 而 **mp4v2 结构上不支持**, 且会推翻"断电残留靠侧车裸流救"的整套设计(见 §2 与 B034 的调研)。③ 内存里攒一段再落盘: 一段几十 MB, 板上没有这个余量 |
| **开机自动把残留侧车重封成 MP4**(`recover_leftovers()`) | 侧车若只能"拷到 PC 上转",就等于**没有**掉电保护 —— 板子自己转, 用户拿到手就是能播的文件。复用与正常录制**同一套** mp4v2 调用(不是第二套封装代码),所以结果与录像完全一致 | 这一步**同步跑在录制线程里**,期间实时帧会被队列丢掉(实测 81 MB → 13 秒 → 丢 173 帧);升级路径 = 改成独立低优先级线程,已用 `【简化上限】` 标注 |

---

## 三、已知问题与技术债(诚实清单)

> 这一节是**故意公开**的:一个能说清"哪里还没做好"的项目,比一个声称全都做好的项目可信。

| # | 问题 | 影响 | 计划 |
|---|---|---|---|
| 1 | ~~**RTSP over TCP(RTP 交错)一帧都发不出去**~~ | ✅ **已解决(2026-09-18)**。查出来是**两个**根因:① `svc_sender` 不看传输方式, 一律建 UDP sender(TCP 时往"RTP 端口 0"发);② **更本质** —— 发送主路径 `emit()` 直接调 `proto_rtp_send_nalu()`(里面自己 `sendto`), **绕过了传输抽象**, 所以 TCP sender 建了也不被调用。修法:新增 `infra_transport_t` 由 `svc_net` 交出传输方式 + 把 `proto_rtp` 的"打包/发送"拆开(新增 `proto_rtp_pack_nalu(cb)`, `send_nalu` 保留为薄包装) | 已完成 |
| 2 | ~~**磁盘写满发生在"段中间"时不会清理**~~ | ✅ **已解决(2026-09-18)**:写入失败先判 `errno` —— **只读**(`EROFS`)只报警(删了也没用);其它按"空间不够"处理:删最旧 + 保底预留 + **把同一个 NALU 重试一次**。开新段**之前**还用 `statvfs` 提前把空间腾够。预留 = 见过的最大段 ×(开了侧车则 ×2)× 1.25 | 已完成 |
| 3 | ~~**残缺分段没人清理**~~ | ✅ **已解决(2026-09-18)**:正在写的段改用 `<stamp>.mp4.tmp`,`MP4Close` 成功后才 `rename` 成 `.mp4`,启动时清残留 `*.tmp`;并额外写**旁路裸流侧车** `<stamp>.h264.tmp`(收尾即删),`kill -9` 后能把这一段救回来(实测 **315 帧**) | 已完成 |
| 4 | ~~**只按时间切段,不按大小** —— FAT32 单文件上限 **4 GiB**~~ | ✅ **已解决(2026-09-19)**:时间到**或**大小到, 谁先到算谁(默认 30 分钟 / 1024 MB, 与海康习惯的 1 GB 一致);边界仍对齐 IDR;大小上限**故意不给关闭**(0 只是"用默认"), 设到接近 4 GiB 时启动即告警。实测见 A14 | 已完成 |
| 5 | ~~RTP 时间戳**来自假设帧率**,不是编码器的 PTS~~ | ✅ **已解决(2026-09-15)**:`proto_rtp_session_frame_pts()` 改为按**编码器 PTS** 推进(1 MHz→90 kHz 换算, 不是直接相加);生产路径 `svc_sender.c:291` 真的调它, PTS 来自 `bsp_mpp.c:291` 的 `pstPack[0].u64PTS`。⚠️ **没做上板的时间戳专项验收**(只靠真实客户端能按正常速度播放间接佐证) | 已完成。旧路径 `proto_rtp_session_next_frame()` 保留但已标 `@deprecated`(只有 PC 单测与 `rtp_header_dump` 在用) |
| 6 | ~~**M1-11 多客户端从未用第二个客户端实测**~~ | ✅ **已解决(2026-09-19)**:2×UDP + 1×TCP 交错同时拉 20 秒,三个客户端各自 602 帧可播、录制仍 30.32 fps 不丢帧(见 A13) | 已完成 |
| 7 | ~~**"锁定段保护"**(被锁的段跳过环形删除)未实现~~ | ✅ **已解决(2026-09-19)**:录制目录里一个清单文件 `.locked`(每行一个分段名), 策略层把 `locked` 当**数据**跳过它并**继续删下一个最旧的**;全被锁定时明确报错("已超上限但没有可删的分段")且不停录制。实测见 A15 | 已完成。**"写满即停"仍然明确不做** —— 决定"满了就覆盖最旧的", 这也是**当前行为**(A5/A10 已验证) |
| 8 | ~~**仓库未附构建脚本** —— `README.md` 原来写着 `make`,但仓库里**没有 Makefile**,真正的构建脚本还在开发环境里~~ | ✅ **已解决(2026-09-19)**:仓库自带 `Makefile` —— `make`(交叉编译, 路径全用变量可覆盖)、`make test`(**宿主 gcc** 跑 9 条 PC 单测, 不需要 SDK/板子)、`make clean`、`make help`;README 第六节重写了完整说明。从"只有仓库内容 + 干净目录"实测见 A16 | 已完成 |
| 9 | ~~**"刚录的这一段"仍然看不到**(缺口大小 = 段长)~~ | ✅ **已解决(2026-09-20, A20)**:`GET /recent.mp4` —— 请录制线程在下一个 IDR 处把当前段**收尾**(复用已有切段路径, 一帧不丢), 再把刚收尾的整段发出去;**缺口从"段长(最多 30 分钟)"降到"1 秒左右"**。配套:20 秒"段太新不切"保护(防连点切碎段)、`-no-record` 时如实返回 **503**、页面加了"看最新"按钮。未做:`?sec=N`(只取最近 N 秒)—— 现在给的是"整段";要做就得自己重封, 见下条 | 已完成(A20 上板 17/17)。⚠️ 代价:每次请求多一个分段边界, 并占住服务线程最多 5 秒 |
| 10 | ⭐ **收尾时 `MP4Close()` 的返回值被忽略** —— 紧接着就**无条件 `rename`** 成 `.mp4`(`svc_record.c:812-823`) | 收尾那一刻正好盘满、或卡被内核重挂成只读 ⇒ 目录里会出现一个**真的没有 `moov`、打不开**的 `.mp4`;而 `scan_dir()`(`svc_record.c:413`)只按名字+大小统计、**不校验可播性** ⇒ 它会被回放列表当成正常分段列出来("看着有录像其实播不了") | **待实施(小)**:判 `MP4Close()` 的返回值, 失败就**不改名**(留在 `.tmp`, 或挪进 `bad/` 子目录)。注意 `open_segment()` 已经在开段前腾空间(`ensure_headroom()`)—— 这条补的是"**收尾那一刻**"的洞。⚠️ 来源:2026-09-21 遗留问题整表对账时读代码发现 |

**不夸大地说**:这个项目目前能稳定做到"录出来能播、分段连续、环形覆盖正确、优雅关闭可保全部已关段";
做不到"真断电零丢失"(硬件上没有 RTC/超级电容,软件上 mp4v2 也写不出分片索引)。

---

## 四、行业调研(不是"我觉得", 是有来源的)

就"掉电怎么办、卡满怎么办、客户端怎么做回放"做了 5 路带来源的调研:

- **共 133 条来源,其中官方文档/标准 92 条(69%)**;每份笔记都带
  **来源类型 / 页面日期 / 证据强度分层**(规范原文 → 社区实现源码 → 二手转录)与 **Gaps**。
- 笔记在 [`docs/research/`](research/):掉电保护 / 存储生命周期 / 厂商与标准 / 开源实现 / 客户端与回放。
- **结论汇总表**(行业做法 → 本项目的决定 → 明确不做什么)在
  [`PROJECT_PLAN.md` §3.9](PROJECT_PLAN.md)。

几条值得单独说的调研结论:

1. **ONVIF Profile G 完全不管分段** —— 规范全文里 `Segment`/`Retention`/`Storage`/`Export`
   命中**全为 0**。所以**不能**用"符合 Profile G"来论证分段策略。
2. **行业没有统一的"预留 xx%"水位** —— 流传的"不超过 80%"都出自容量规划建议,
   不是运行时参数。有具体数值的是开源实现:ZoneMinder 出厂 `DiskPercent>=95` + 删最旧;
   Frigate 用"剩余不足约 1 小时录像量"。
3. **"段中间盘满"没有任何开源项目专门处理**(它们都是周期性轮询)——本项目的处理会超出常见实践。
4. **海思官方 sample 零分段逻辑**:只有 `SAMPLE_COMM_VENC_SaveStream` 存**裸码流**,
   全文件**没有任何 MP4 调用** ⇒ **"MP4 封装 + 分段 + 环形覆盖"完全是自己实现的**。

---

## 五、编译与复现

```bash
# 交叉编译(需要海思 SDK 与 arm-himix200 交叉工具链, 参见 README)
# 产物 ipc_app 部署到板子后:
./ipc_app -p 8554 -s 1800 -r 8192     # 默认: 每段 30 分钟 / 环形上限 8 GB

# 拉流(把 <board-ip> 换成板子地址)
ffprobe -rtsp_transport udp -i rtsp://<board-ip>:8554/live
ffplay  -rtsp_transport udp rtsp://<board-ip>:8554/live
```

| 选项 | 说明 |
|---|---|
| `-p <端口>` | RTSP 端口(默认 8554) |
| `-s <秒>` | 每段多少秒切新文件(默认 **1800 = 30 分钟**) |
| `-r <MB>` | 环形容量上限(默认 **8192**;⚠️ **必须大于一段**) |
| `-no-record` | 不录 MP4 |
| `-no-osd` | 不叠时间水印 |
| `-h265` | 取 H.265 那一路(默认 H.264 720p)。⚠️ mp4v2 不支持 H.265 封装,该模式下**录制会自动关闭** |
| `-H <端口>` | **回放服务(HTTP)端口**(默认 **8080**;`-no-http` 可整个关掉) |
| `-no-http` | 不起 HTTP 回放服务(只留 RTSP 推流) |
| `-L <端口>` | **实时画面(MJPEG)端口**(默认 **8081**;`-no-live` 可关掉) |
| `-no-live` | 不起实时画面服务(回放/推流/录制照常) |
| `-v` | 打开 DEBUG 日志 |

回放服务(阶段 2,`http://<board-ip>:8080/`):

| 路径 | 说明 |
|---|---|
| `/` | 板子自己发的**回放页面**(分段列表 + `<video>` + 时间轴 + 锁定/另存 + "● 正在录"横幅, **每 5 秒自刷新**) |
| `/status` | **正在录**的那一段:名字 / 已写字节 / 帧数 / 起始时间 / 直播地址(JSON) |
| `/recent.mp4` | **"刚录的这段"立刻能看**:请录制线程把当前段收尾, 再发刚收尾的那一整段(支持 `Range`);应答头 `X-Recent-Clip` 告诉你拿到的是哪一段;没在录 ⇒ 503 |
| `/status` 里的 `live_mjpg` | **实时画面(MJPEG)的 URL**(形如 `http://<板子>:8081/live.mjpg`)—— 页面用它显示 `<img>` |

实时画面走**另一个端口**(默认 8081, `-L` 改、`-no-live` 关):

| 路径 | 说明 |
|---|---|
| `/live.mjpg` | **MJPEG 流**(`multipart/x-mixed-replace`):浏览器 `<img src=...>` 直接显示, **零安装**;`/live`、`/stream` 等价 |
| `/help` 等其它路径 | 404(它不是回放服务, 只有这一条流) |
| `/playlist.m3u` | 给 VLC/mpv 的**整段**播放列表(旧→新, 绝对 URL) |
| `/recordings` | 分段 JSON 列表, **默认最新 64 条**, `?limit=&before=` 翻页 |
| `/recordings/<名字>` | 取某一段(GET/HEAD, 支持 `Range` → 206) |
| `/recordings/<名字>/lock` | `PUT` body `1` 锁 / `0` 解锁(被锁的段环形覆盖不删) |
| `/help` | 纯文本接口说明 |

---

### 板上安装与开机自启(rootfs,**定稿时做一次**)

开发时用的是"NFS → `/tmp/ipc_app` 跑一份"(**零 flash 写入**)。要让板子**上电即工作**,
就把二进制**安装**进 rootfs 一次(概念上是"安装",不是"每次部署"):

| 步骤 | 做什么 | 判据(都是产物) |
|---|---|---|
| ① | **重新编译**(不要装上一轮的产物 —— 这个坑踩过) | 我们自己的代码 **0 告警** |
| ② | `cp <nfs>/ipc_app /usr/bin/ipc_app && chmod 755; sync` | 板上的 **`md5sum` 与编译产物一致** |
| ③ | **重启**后核对 | `/tmp/ntpd.log` 有 `ntp ok … (uptime xx.xs)`;`/tmp/ipc_app.boot.log` 里有**启动的那个二进制的 md5**;`/status` 的段名是**本地时间**(不是 `1970-…`);`netstat -ltn` 里 8554/8080/8081 在听 |

实测开销:5,068,456 字节的二进制在 jffs2 上占 **2,552 KB**(装完根文件系统剩 **7,880 KB**)。

rcS 末尾挂着四个托管块(`BEGIN/END` 标记,幂等):

- **`ntp-sync`**:板子**没有 RTC**(断电后从 1970 起),开机同步时间。
  ⚠️ **判据必须是 `date +%Y`,不能是 `ntpd` 的退出码** —— busybox 的 `ntpd -q`
  **不等时钟被设就返回 0**,用退出码判断会打出假的成功、还会把重试循环提前 break 掉。
- **`sdcard-mount`**:⚠️ **必须重试**。MMC 是异步探测的,开机那一刻设备节点
  `/dev/mmcblk0p1` 可能还没建出来 ⇒ 单次 `[ -b ... ]` 检查会判定"没插卡"而不挂载
  (实测**每次开机第一次都失败**)。日志 `/tmp/sdcard.log`。
- **`nfs-mount`**:同样重试(单次 ping 在链路刚就绪时会失败)。
- **`app-autostart`**:先**等时钟正常 + 等 TF 卡真的挂上**(各自最多 120 秒)再起 app ——
  否则第一段录像的文件名会是 `1970-01-01-…`;**TF 卡没挂上就带 `-no-record` 启动**
  (照常推流/回放, **绝不把录像写进 flash**);必带 `TZ=CST-8`(OSD 水印走 `localtime_r()`,
  不给 `TZ` 就显示 UTC);并把启动的那个二进制的**大小 + md5 + 启动选项**
  写进 `/tmp/ipc_app.boot.log`。
